import React from 'react';
import BoardList from '../components/BoardList';

const About = ({ user }) => {
    return (
        <div>
            <BoardList user={user} />

        </div>
    )
}

export default About
